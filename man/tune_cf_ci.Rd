% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_tree_control_grid.R
\name{tune_cf_ci}
\alias{tune_cf_ci}
\title{Tune greedy concentration-index forest controls by cross-validation}
\usage{
tune_cf_ci(
  formula,
  data,
  rank_name,
  outcome_name,
  weights = NULL,
  type = c("CI", "CIg", "CIc"),
  control_grid = NULL,
  ntree = 500L,
  v = 5L,
  strata = NULL,
  fold_id = NULL,
  seed = NULL,
  surrogate_control = NULL,
  prediction_name = "forest_risk",
  refit = TRUE,
  verbose = FALSE,
  perturb = list(replace = FALSE, fraction = 0.632),
  na.action = stats::na.omit,
  ...
)
}
\arguments{
\item{formula}{A model formula, typically with a two-column response such as
\code{cbind(rank, outcome) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{rank_name}{Name of the socioeconomic rank variable.}

\item{outcome_name}{Name of the outcome variable.}

\item{weights}{Optional non-negative case weights.}

\item{type}{One or more of \code{"CI"}, \code{"CIg"}, or \code{"CIc"} selecting candidate
concentration-index objectives.}

\item{control_grid}{A data frame of candidate controls. Defaults to
\code{\link[=ci_tree_control_grid]{ci_tree_control_grid()}}. If it contains a \code{type} column, that column is
used instead of expanding over \code{type}.}

\item{ntree}{Number of trees used when \code{control_grid} does not contain an
\code{ntree} column.}

\item{v}{Number of cross-validation folds.}

\item{strata}{Optional stratum vector, or a single column name in \code{data},
used for stratified fold creation.}

\item{fold_id}{Optional precomputed integer fold ids. When supplied, \code{v},
\code{strata}, and \code{seed} are ignored for fold creation.}

\item{seed}{Optional random seed for fold creation.}

\item{surrogate_control}{Optional \code{\link[=ci_tree_control]{ci_tree_control()}} for the surrogate tree.
By default, the selected forest controls are reused with \code{mtry = NULL} so
the surrogate searches all split variables.}

\item{prediction_name}{Name of the forest-prediction column added to the
surrogate data.}

\item{refit}{Should the best setting be refitted on the full data?}

\item{verbose}{Should fold progress be printed?}

\item{perturb}{Resampling controls passed to \code{\link[=cf_ci]{cf_ci()}}.}

\item{na.action}{A function for handling missing values.}

\item{...}{Additional arguments passed to \code{\link[=ctree_ci]{ctree_ci()}}.}
}
\value{
A list with fold-level results, grid summary, best parameters, best
concentration-index type, selected forest, selected surrogate tree, and
surrogate data.
}
\description{
Fits \code{\link[=cf_ci]{cf_ci()}} across candidate forest controls and concentration-index
criteria. Each validation forest is summarized by a surrogate greedy
\code{\link[=ctree_ci]{ctree_ci()}} fitted to the forest predictions on the training fold, then
scored by held-out concentration-index validation gain against the observed
outcome. This keeps forest tuning aligned with the inequality objective while
returning both the selected forest and a readable surrogate tree.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60, 70, 80),
  outcome = c(1, 0, 1, 0, 1, 1, 0, 1),
  income = c(2, 4, 6, 8, 10, 12, 14, 16)
)
grid <- ci_tree_control_grid(
  minsplit = 1,
  minbucket = 1,
  minprob = 0,
  maxdepth = 1,
  mtry = 1,
  ntree = 3
)
tuned <- tune_cf_ci(
  cbind(rank, outcome) ~ income,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control_grid = grid,
  type = c("CI", "CIg"),
  v = 2,
  seed = 1
)
tuned$best_type

}
