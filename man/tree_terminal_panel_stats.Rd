% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_default_name.R
\name{tree_terminal_panel_stats}
\alias{tree_terminal_panel_stats}
\title{Create a terminal-panel generator for tree plots with node summaries}
\usage{
tree_terminal_panel_stats(
  obj,
  stats_dt,
  node_col = "node_id",
  stat_labels = NULL,
  stat_formatters = list(),
  gp = NULL,
  fill = "lightgray",
  width_lines = 10.5,
  height_lines = 3.2
)
}
\arguments{
\item{obj}{A fitted tree object inheriting from \code{party}.}

\item{stats_dt}{A data frame of terminal-node summaries, typically created by
\code{\link[=tree_build_terminal_stats]{tree_build_terminal_stats()}}.}

\item{node_col}{Name of the column in \code{stats_dt} storing terminal node ids.}

\item{stat_labels}{An optional named character vector of display labels for
summary columns.}

\item{stat_formatters}{An optional named list of formatting functions for
summary values.}

\item{gp}{An optional \code{grid::gpar()} object applied to the node viewport.}

\item{fill}{Fill color used for the terminal node box.}

\item{width_lines}{Width of the terminal box in \code{grid} line units.}

\item{height_lines}{Height of the terminal box in \code{grid} line units.}
}
\value{
A graph-content generator function suitable for the \code{terminal_panel}
argument of \code{\link[partykit:party-plot]{partykit::plot.party()}}.
}
\description{
Builds a \code{partykit} graph-content generator that draws terminal nodes using
precomputed summary statistics, optional display labels, and optional value
formatters.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12),
  group = factor(c("low", "low", "mid", "mid", "high", "high"))
)

fit <- ctree_ci(
  formula = cbind(rank, outcome) ~ income + group,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control = partykit::ctree_control(mincriterion = 0, maxdepth = 1)
)

stats_df <- tree_build_terminal_stats(
  fit,
  toy_data,
  stat_funs = list(
    n = nrow,
    mean_outcome = function(df) mean(df$outcome)
  )
)

inherits(tree_terminal_panel_stats, "grapcon_generator")
is.function(tree_terminal_panel_stats(fit, stats_df))
}
