% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_ci_tree_terminal_mean.R
\name{ci_forest_surrogate}
\alias{ci_forest_surrogate}
\title{Fit a surrogate CI tree for a CI forest}
\usage{
ci_forest_surrogate(
  forest_fit,
  data = NULL,
  formula = NULL,
  rank_name = NULL,
  weights = NULL,
  type = NULL,
  control = NULL,
  prediction_name = "forest_risk",
  na.action = stats::na.omit
)
}
\arguments{
\item{forest_fit}{A fitted \code{ci_forest} object.}

\item{data}{Optional data used to fit the surrogate. Defaults to the forest
training data stored in \code{forest_fit}.}

\item{formula}{Optional formula defining the surrogate predictors. Defaults
to the formula stored in \code{forest_fit}, with the original outcome replaced
by \code{prediction_name}.}

\item{rank_name}{Optional rank column name. Defaults to \code{forest_fit$rank_name}.}

\item{weights}{Optional non-negative case weights for \code{data}. Defaults to
the forest training weights when \code{data} has the same number of rows as the
original training data, otherwise equal weights.}

\item{type}{Optional concentration-index type. Defaults to \code{forest_fit$type}.}

\item{control}{Optional \code{\link[=ci_tree_control]{ci_tree_control()}} for the surrogate. Defaults to
the forest controls with \code{mtry = NULL}.}

\item{prediction_name}{Name of the prediction column added to the surrogate
data.}

\item{na.action}{A function for handling missing values in the surrogate
tree fit.}
}
\value{
A list with \code{fit}, \code{data}, and \code{prediction_name}.
}
\description{
Predicts from a fitted \code{\link[=ci_forest]{ci_forest()}} object and fits a greedy \code{\link[=ci_tree]{ci_tree()}} to
the forest predictions. The result is the same kind of surrogate used
internally by \code{\link[=tune_ci_forest]{tune_ci_forest()}} to score forest tuning settings by
concentration-index validation gain.
}
