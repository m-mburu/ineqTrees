% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_tree_control_grid.R
\name{predict_ctree_ci_terminal_mean}
\alias{predict_ctree_ci_terminal_mean}
\title{Predict terminal-node outcome means from a greedy CI tree}
\usage{
predict_ctree_ci_terminal_mean(
  fit,
  train_data,
  new_data = train_data,
  outcome_name,
  weights = NULL
)
}
\arguments{
\item{fit}{A fitted \code{ci_tree} or other object inheriting from \code{party}.}

\item{train_data}{Training data used to compute terminal-node means.}

\item{new_data}{Data for which predictions should be returned. Defaults to
\code{train_data}.}

\item{outcome_name}{Name of the numeric or logical outcome column in
\code{train_data}.}

\item{weights}{Optional non-negative training weights.}
}
\value{
A numeric vector of predicted terminal-node means.
}
\description{
Assigns training observations to terminal nodes, computes a weighted mean
outcome in each node, and returns those node means for \code{new_data}. This is a
simple prediction rule for scoring \code{\link[=ctree_ci]{ctree_ci()}} models when the tree itself
is primarily grown to reduce within-node inequality rather than prediction
error.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12)
)
fit <- ctree_ci(
  cbind(rank, outcome) ~ income,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control = ci_tree_control(minsplit = 1, minbucket = 1, maxdepth = 1)
)
predict_ctree_ci_terminal_mean(fit, toy_data, outcome_name = "outcome")

}
