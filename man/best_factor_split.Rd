% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_wt.R
\name{best_factor_split}
\alias{best_factor_split}
\title{Find the best factor split by concentration-index gain}
\usage{
best_factor_split(x_full, keep, y_full, wt_full, varid, ctrl, ci_fun)
}
\arguments{
\item{x_full}{A factor predictor containing all original levels.}

\item{keep}{A logical vector selecting the observations currently available
for splitting.}

\item{y_full}{A two-column numeric matrix whose first column is the
socioeconomic ranking variable and whose second column is the health
outcome.}

\item{wt_full}{A numeric vector of case weights for all observations.}

\item{varid}{Integer identifier of the splitting variable, passed to
\code{\link[partykit:partysplit]{partykit::partysplit()}}.}

\item{ctrl}{A list-like control object containing \code{minbucket} and \code{minprob}.}

\item{ci_fun}{A concentration-index scoring function, typically created by
\code{\link[=ci_factory]{ci_factory()}}.}
}
\value{
A \code{partysplit} object describing the best admissible split of the
observed factor levels, or \code{NULL} if fewer than two levels are present
after subsetting or if all candidate splits violate the control
constraints.
}
\description{
Evaluate cumulative binary splits of a factor predictor after ordering the
observed levels by weighted mean outcome, and return the split with the
largest weighted concentration-index gain.
}
\details{
Only levels present among the kept observations are scored, but the returned
split keeps the full level mapping so it can be used with
\code{\link[partykit:partysplit]{partykit::partysplit()}}.
}
\examples{
x_full <- factor(
  c("low", "mid", "high", "low", "mid", "high"),
  levels = c("low", "mid", "high", "unused")
)
keep <- rep(TRUE, 6)
y_full <- cbind(
  ses = c(10, 20, 30, 40, 50, 60),
  health = c(1, 2, 5, 1, 2, 5)
)
wt_full <- rep(1, 6)
ctrl <- list(minbucket = 1, minprob = 0)

split <- best_factor_split(
  x_full = x_full,
  keep = keep,
  y_full = y_full,
  wt_full = wt_full,
  varid = 1,
  ctrl = ctrl,
  ci_fun = ci_factory("CI")
)
split$index
}
