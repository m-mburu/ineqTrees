% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_ci_tree_terminal_mean.R
\name{ci_forest_validation_gain}
\alias{ci_forest_validation_gain}
\title{Compute held-out concentration-index validation gain for a CI forest}
\usage{
ci_forest_validation_gain(
  fit,
  new_data,
  rank_name = fit$rank_name,
  outcome_name = fit$outcome_name,
  weights = NULL,
  type = fit$type,
  root_impurity = NULL
)
}
\arguments{
\item{fit}{A fitted \code{ci_forest} object.}

\item{new_data}{Validation data.}

\item{rank_name}{Name of the socioeconomic ranking column.}

\item{outcome_name}{Name of the outcome column.}

\item{weights}{Optional non-negative validation weights.}

\item{type}{One of \code{"CI"}, \code{"CIg"}, \code{"CIc"}, or \code{"L"} selecting the
inequality index used for validation scoring. \code{"L"} uses observed
socioeconomic levels in the first response column rather than fractional
ranks.}

\item{root_impurity}{Optional pre-computed root impurity for the validation
sample under the selected concentration-index criterion. If \code{NULL}, the
root impurity is computed from \code{new_data}.}
}
\value{
A single numeric validation gain.
}
\description{
Applies each member tree in a fitted \code{\link[=ci_forest]{ci_forest()}} to validation data,
computes \code{\link[=ci_tree_validation_gain]{ci_tree_validation_gain()}} for each internal tree partition, and
returns the average gain across trees. This is a forest-native partition
diagnostic: it measures the average concentration-index impurity reduction
from the forest's stored trees, not from a surrogate tree and not from the
averaged prediction surface.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60, 70, 80),
  outcome = c(1, 0, 1, 0, 1, 1, 0, 1),
  income = c(2, 4, 6, 8, 10, 12, 14, 16)
)
forest <- ci_forest(
  cbind(rank, outcome) ~ income,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  ntree = 3,
  control = ci_tree_control(minsplit = 1, minbucket = 1, maxdepth = 1)
)
ci_forest_validation_gain(forest, toy_data, "rank", "outcome")

}
