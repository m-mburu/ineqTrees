% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_default_name.R
\name{tree_edge_panel_compact}
\alias{tree_edge_panel_compact}
\title{Create a compact edge-panel generator for tree plots}
\usage{
tree_edge_panel_compact(
  obj,
  var_labels = NULL,
  digits = 3,
  fill = "white",
  justmin = 4,
  just = c("alternate", "increasing", "decreasing", "equal"),
  max_items = 3L,
  max_chars = 24L,
  plural_overrides = NULL
)
}
\arguments{
\item{obj}{A fitted tree object inheriting from \code{party}.}

\item{var_labels}{An optional named character vector of display labels.}

\item{digits}{Number of digits to use when formatting numeric split labels.}

\item{fill}{Fill color used behind edge labels.}

\item{justmin}{Minimum average label width before alternating justification
is used.}

\item{just}{One of \code{"alternate"}, \code{"increasing"}, \code{"decreasing"}, or
\code{"equal"} controlling vertical placement of sibling edge labels.}

\item{max_items}{Maximum number of comma-separated items to show before
shortening the label.}

\item{max_chars}{Maximum wrapping width used for unshortened labels.}

\item{plural_overrides}{An optional named character vector giving custom
plural nouns for summarized level groups.}
}
\value{
A graph-content generator function suitable for the \code{edge_panel}
argument of \code{\link[partykit:party-plot]{partykit::plot.party()}}.
}
\description{
Builds a \code{partykit} graph-content generator that draws compact edge labels
using \code{\link[=tree_compact_split_label]{tree_compact_split_label()}}. This is useful when factor splits have
many levels or when tree plots need shorter, more readable split labels.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12),
  group = factor(c("low", "low", "mid", "mid", "high", "high"))
)

fit <- ctree_ci(
  formula = cbind(rank, outcome) ~ income + group,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control = partykit::ctree_control(mincriterion = 0, maxdepth = 1)
)

inherits(tree_edge_panel_compact, "grapcon_generator")
is.function(tree_edge_panel_compact(fit))
}
