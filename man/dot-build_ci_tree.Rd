% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_tree_control.R
\name{.build_ci_tree}
\alias{.build_ci_tree}
\title{Recursively build a greedy concentration-index party tree}
\usage{
.build_ci_tree(data, y, wt, vars, ctrl, ci_fun, type = "CI")
}
\arguments{
\item{data}{Data frame of predictor variables.}

\item{y}{Two-column numeric matrix with \code{rank} and \code{outcome}.}

\item{wt}{Non-negative case weights.}

\item{vars}{Integer positions of predictor variables considered for
splitting.}

\item{ctrl}{Normalized control list from \code{.ci_tree_normalize_control()}.}

\item{ci_fun}{Concentration-index function created by \code{ci_factory()}.}

\item{type}{Concentration-index type used when \code{control$split_engine} is
\code{"cpp"}.}
}
\value{
A list with \code{node}, the root \code{partykit::partynode()}, and \code{fitted},
an integer vector mapping training rows to terminal node ids.
}
\description{
Grows the tree by repeatedly calling \code{best_global_ci_split()} at each node,
so every split is chosen by directly maximizing concentration-index gain over
candidate variables and candidate split points. The returned node is a
\code{partykit} node and can be wrapped by \code{partykit::party()}.
}
