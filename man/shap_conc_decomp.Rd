% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shap_conc_decomp.R
\name{shap_conc_decomp}
\alias{shap_conc_decomp}
\title{Decompose a concentration index using SHAP values}
\usage{
shap_conc_decomp(
  shap,
  rank,
  type = c("CI", "L"),
  prediction = NULL,
  baseline = NULL,
  from_dalex = FALSE,
  dalex_id_col = NULL,
  dalex_variable_col = NULL,
  dalex_value_col = NULL,
  na_rm = FALSE,
  sort = TRUE,
  tolerance = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{shap}{A rectangular SHAP table. By default this should be a wide table
with one numeric feature column per SHAP contributor. If \code{from_dalex = TRUE}, \code{shap} should instead be a long DALEX-like table containing
observation IDs, feature names, and SHAP values.}

\item{rank}{A numeric vector giving the socioeconomic ranking variable for
each observation. When \code{type = "L"}, this is interpreted as the observed
socioeconomic level variable.}

\item{type}{One of \code{"CI"} or \code{"L"} selecting the decomposition target. \code{"CI"}
uses fractional socioeconomic ranks. \code{"L"} uses the Erreygers--Kessels
level-dependent bivariate index based on observed socioeconomic levels.}

\item{prediction}{Optional numeric vector of model predictions. If omitted,
predictions are reconstructed as \code{baseline + rowSums(shap)}.}

\item{baseline}{Optional scalar baseline prediction. Required when
\code{prediction} is not supplied.}

\item{from_dalex}{Logical scalar. If \code{TRUE}, reshape a DALEX-like long SHAP
table to wide format before decomposition.}

\item{dalex_id_col}{Optional column name identifying observations in the
DALEX-like input.}

\item{dalex_variable_col}{Optional column name containing feature names in
the DALEX-like input.}

\item{dalex_value_col}{Optional column name containing SHAP values in the
DALEX-like input.}

\item{na_rm}{Logical scalar. If \code{TRUE}, drop rows with missing values in
required inputs before computing the decomposition.}

\item{sort}{Logical scalar. If \code{TRUE}, order output features by decreasing
absolute SHAP contribution.}

\item{tolerance}{Numeric tolerance used to detect numerically zero mean
predictions or concentration indices.}
}
\value{
A list of class \code{"shap_ci_decomposition"} with two components:
\code{diagnostics}, a one-row \code{data.table} of decomposition diagnostics, and
\code{contributions}, a \code{data.table} containing one row per SHAP feature with
columns \code{feature}, \code{D_k_SHAP}, \code{pct_contribution}, and
\code{abs_contribution}.
}
\description{
Compute SHAP-based feature contributions to the concentration index of a
predicted outcome. The SHAP table can be supplied either in wide format
(one row per observation, one column per feature) or in DALEX-like long
format and reshaped internally.
}
\details{
For \code{type = "CI"}, each feature \code{k}, \code{D_k_SHAP} is computed as
\code{2 / (n * mu_hat)} times the sum of \code{phi_ik * (R_i - 0.5)} across
observations, where \code{phi_ik} is the SHAP value for feature \code{k}, \code{R_i} is
the fractional rank of the socioeconomic ordering variable, and \code{mu_hat}
is the mean predicted outcome.

For \code{type = "L"}, \code{rank} is treated as an observed socioeconomic level
\code{s_i}. The decomposition uses \code{mean(((s_i - mu_s) / mu_s) * phi_ik)} for
each feature, where \code{mu_s} is the mean socioeconomic level.
}
\examples{
shap <- data.frame(
  education = c(1, 0, -1),
  water = c(0, 1, 1)
)
rank <- c(1, 2, 3)

res <- shap_conc_decomp(
  shap = shap,
  rank = rank,
  baseline = 2
)
res$diagnostics
res$contributions

dalex_like <- data.frame(
  "_id_" = rep(1:3, each = 3),
  "_vname_" = rep(c("_baseline_", "education", "water"), times = 3),
  "_attribution_" = c(2, 1, 0, 2, 0, 1, 2, -1, 1),
  check.names = FALSE
)

shap_conc_decomp(
  shap = dalex_like,
  rank = rank,
  baseline = 2,
  from_dalex = TRUE
)$contributions
}
