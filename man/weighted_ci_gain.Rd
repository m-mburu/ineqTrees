% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_wt_retained.R
\name{weighted_ci_gain}
\alias{weighted_ci_gain}
\title{Compute weighted concentration-index gain for a binary split}
\usage{
weighted_ci_gain(y, wt, left, ci_fun)
}
\arguments{
\item{y}{A matrix with two columns: SES rank variable and health outcome.}

\item{wt}{A numeric vector of weights.}

\item{left}{A logical vector indicating the left split.}

\item{ci_fun}{A function to compute the concentration index.}
}
\value{
The weighted concentration-index gain.
}
\description{
Compute the reduction in concentration-index impurity obtained by splitting
a parent node into left and right child nodes. The gain is the parent node's
concentration-index score minus the weighted average of the two child-node
scores.
}
\details{
For concentration-index impurity \code{I_m(.)} based on index variant
\code{m}, parent node \code{t}, and a candidate split creating left and right child
nodes \code{t_L} and \code{t_R}, the gain is
\deqn{G_m(t, j, s) =
  I_m(t) - \frac{W_L}{W_t} I_m(t_L) -
  \frac{W_R}{W_t} I_m(t_R)}
where \code{W_t}, \code{W_L}, and \code{W_R} are the total case weights in the parent,
left child, and right child. Positive gain means the split reduces the
weighted within-node concentration-index impurity.

In the greedy tree builder, the selected split at node \code{t} is the
admissible variable-split pair with the largest gain:
\deqn{(j^*, s^*) =
  \arg\max_{j \in \mathcal{J}_t,\ s \in \mathcal{S}_{jt}}
  G_m(t, j, s).}
}
\references{
Breiman L, Friedman JH, Olshen RA, Stone CJ (1984). \emph{Classification and
Regression Trees}. Wadsworth.

Wagstaff A, van Doorslaer E, Watanabe N (2003). "On Decomposing the Causes
of Health Sector Inequalities with an Application to Malnutrition
Inequalities in Vietnam." \emph{Journal of Econometrics}, 112(1), 207-223.
doi:10.1016/S0304-4076(02)00161-6.
}
