% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert_data_table_available.R
\name{.prepare_named_shap_dt}
\alias{.prepare_named_shap_dt}
\title{Prepare a SHAP table with named numeric columns
Ensure that the input SHAP table is a data.table with named numeric columns.
If the input is coercible to a data.table but has no column names,
assign default names
like "feature_1", "feature_2", etc. Validate that all columns are numeric and
optionally check for missing values.}
\usage{
.prepare_named_shap_dt(shap, allow_missing = FALSE)
}
\arguments{
\item{shap}{A SHAP table to prepare.}

\item{allow_missing}{Logical scalar. If \code{TRUE}, allow missing values in
SHAP columns. If \code{FALSE}, trigger an error if any SHAP column contains
missing values.}
}
\value{
A data.table with named numeric columns representing SHAP features.
}
\description{
Prepare a SHAP table with named numeric columns
Ensure that the input SHAP table is a data.table with named numeric columns.
If the input is coercible to a data.table but has no column names,
assign default names
like "feature_1", "feature_2", etc. Validate that all columns are numeric and
optionally check for missing values.
}
\details{
This function is used internally to standardize SHAP tables
before decomposition. It ensures that the SHAP data is in a consistent
format with appropriate column names and types.
}
