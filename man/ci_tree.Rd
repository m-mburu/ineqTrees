% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_tree.R
\name{ci_tree}
\alias{ci_tree}
\alias{ctree_ci}
\title{Fit a greedy concentration-index tree}
\usage{
ci_tree(
  formula,
  data,
  rank_name,
  outcome_name,
  weights = NULL,
  type = c("CI", "CIg", "CIc", "L"),
  control = ci_tree_control(),
  na.action = stats::na.omit,
  ...
)

ctree_ci(...)
}
\arguments{
\item{formula}{A model formula, typically with a two-column response such as
\code{cbind(rank, outcome) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{rank_name}{Name of the socioeconomic rank variable.}

\item{outcome_name}{Name of the outcome variable.}

\item{weights}{Optional non-negative numeric case weights.}

\item{type}{One of \code{"CI"}, \code{"CIg"}, \code{"CIc"}, or \code{"L"} selecting the
inequality index used for split scoring. \code{"L"} uses observed
socioeconomic levels in the first response column rather than fractional
ranks.}

\item{control}{A control object created by \code{\link[=ci_tree_control]{ci_tree_control()}}. Objects from
\code{\link[partykit:ctree_control]{partykit::ctree_control()}} are also accepted for shared controls such as
\code{minsplit}, \code{minbucket}, \code{minprob}, \code{maxdepth}, and \code{mtry}.}

\item{na.action}{A function for handling missing values.}

\item{...}{Currently ignored; retained for backwards compatibility.}
}
\value{
A fitted \code{ci_tree} object inheriting from \code{constparty} and \code{party}.

A fitted \code{ci_tree} object inheriting from \code{constparty} and \code{party}.
}
\description{
Grow a binary tree by directly maximizing concentration-index gain across
all candidate variables and split points at each node, while storing the
result as a standard \code{\link[partykit:party]{partykit::party()}} object.

\code{ctree_ci()} is kept for existing code. New code should use \code{\link[=ci_tree]{ci_tree()}},
because the current tree-growing method is greedy concentration-index
splitting rather than conditional-inference splitting.
}
\details{
This helper is intended for inequality-aware tree fitting where the response
contains a socioeconomic ranking variable and a health outcome. Unlike
\code{\link[partykit:ctree]{partykit::ctree()}}, it does not use conditional-inference tests for
first-stage variable selection.

At each non-terminal node \code{t}, \code{ci_tree()} searches across all
candidate variables and all admissible split points, then selects the pair
with the largest concentration-index gain:
\deqn{(j^*, s^*) =
\arg\max_{j \in \mathcal{J}_t,\ s \in \mathcal{S}_{jt}}
G_m(t, j, s).}
The gain \code{G_m} is computed by \code{\link[=weighted_ci_gain]{weighted_ci_gain()}} from the selected
concentration-index impurity \code{m}, one of \code{"CI"}, \code{"CIg"}, \code{"CIc"}, or
\code{"L"}.

The fitted object uses \code{\link[partykit:party]{partykit::party()}} for storage, traversal,
prediction, and plotting, but the tree-growing rule is the package's greedy
concentration-index rule rather than the conditional-inference test-based
rule used by \code{\link[partykit:ctree]{partykit::ctree()}}.

Stopping rules operate in three places. First, a node is searched only when
it satisfies \code{minsplit} and has not reached \code{maxdepth}. Second, candidate
splits are admissible only when both child nodes satisfy \code{minbucket} and
\code{minprob}. Third, after all admissible splits are scored, the best split is
accepted only when its gain is finite and greater than \code{min_gain}.
}
\examples{
data(kenya, package = "ineqTrees")

kenya_model_vars <- c(
  "wealth",
  "deadu5_num",
  "rural",
  "ed",
  "reg",
  "unskilled"
)

kenya_model_data <- kenya[
  stats::complete.cases(kenya[, kenya_model_vars]),
  kenya_model_vars
]

set.seed(20260512)
kenya_model_data <- kenya_model_data[
  sample.int(nrow(kenya_model_data), 800L),
  ,
  drop = FALSE
]

fit <- ci_tree(
  formula = cbind(wealth, deadu5_num) ~ rural + ed + reg + unskilled,
  data = kenya_model_data,
  rank_name = "wealth",
  outcome_name = "deadu5_num",
  control = ci_tree_control(
    minsplit = 100,
    minbucket = 50,
    minprob = 0.05,
    maxdepth = 3
  )
)

fit
ci_tree_terminal_summary(fit)
}
\references{
Breiman L, Friedman JH, Olshen RA, Stone CJ (1984). \emph{Classification and
Regression Trees}. Wadsworth.

Hothorn T, Zeileis A (2015). "partykit: A Modular Toolkit for Recursive
Partytioning in R." \emph{Journal of Machine Learning Research}, 16, 3905-3909.
https://jmlr.org/papers/v16/hothorn15a.html.

Wagstaff A, van Doorslaer E, Watanabe N (2003). "On Decomposing the Causes
of Health Sector Inequalities with an Application to Malnutrition
Inequalities in Vietnam." \emph{Journal of Econometrics}, 112(1), 207-223.
doi:10.1016/S0304-4076(02)00161-6.
}
