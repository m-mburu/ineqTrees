% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert_data_table_available.R
\name{.dalex_shap_to_wide}
\alias{.dalex_shap_to_wide}
\title{Reshape a DALEX-like long SHAP table to wide format}
\usage{
.dalex_shap_to_wide(
  shap,
  id_col = NULL,
  variable_col = NULL,
  value_col = NULL,
  drop_baseline = TRUE,
  baseline_labels = c("_baseline_", "baseline", "intercept")
)
}
\arguments{
\item{shap}{A long-format SHAP table containing observation IDs, feature
names, and SHAP values.}

\item{id_col}{Optional column name identifying observations. If \code{NULL},
common ID column names will be searched for.}

\item{variable_col}{Optional column name containing feature names. If
\code{NULL}, common variable column names will be searched for.}

\item{value_col}{Optional column name containing SHAP values. If
\code{NULL}, common value column names will be searched for.}

\item{drop_baseline}{Logical scalar. If \code{TRUE}, rows with feature names
matching common baseline labels will be dropped before reshaping.}

\item{baseline_labels}{Character vector of feature names to treat as
baseline rows when \code{drop_baseline = TRUE}. Matching is case-sensitive.}
}
\value{
A wide-format \code{data.table} with one row per observation and one
numeric column per SHAP feature.
}
\description{
Convert a long-format SHAP table with observation IDs, feature names, and
SHAP values to a wide format with one row per observation and one column per
feature. Baseline rows can be optionally dropped based on common baseline
labels.
}
\details{
The function attempts to identify the ID, variable, and value
columns based on common naming conventions. If \code{drop_baseline = TRUE},
rows with feature names in \code{baseline_labels} are removed before
reshaping. The resulting wide table contains only numeric SHAP feature
columns. Aliases for the identified columns are as follows:
\itemize{
\item ID column candidates: \code{"_id_"}, \code{"id"}, \code{"row_id"},
\code{"observation_id"}, \code{"observation"}, \code{"case_id"}
\item Variable column candidates: \code{"_vname_"}, \code{"variable"},
\code{"variable_name"}, \code{"feature"}
\item Value column candidates: \code{"_attribution_"}, \code{"contribution"},
\code{"phi"}, \code{"shap"}, \code{"value"}
}
}
