% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_forest.R
\name{ci_tree_terminal_summary}
\alias{ci_tree_terminal_summary}
\title{Summarize terminal nodes from a greedy concentration-index tree}
\usage{
ci_tree_terminal_summary(object)
}
\arguments{
\item{object}{A fitted \code{ci_tree} object.}
}
\value{
A \code{data.table} with one row per terminal node.
}
\description{
Extracts a compact table of terminal-node diagnostics from a fitted
\code{\link[=ci_tree]{ci_tree()}} model. The table reports node sample size, weighted size,
depth, node concentration index, and the node-level mean outcome.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12)
)
fit <- ci_tree(
  cbind(rank, outcome) ~ income,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control = ci_tree_control(minsplit = 1, minbucket = 1, maxdepth = 1)
)
ci_tree_terminal_summary(fit)

toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12),
  group = factor(c("low", "low", "mid", "mid", "high", "high"))
)

fit <- ci_forest(
  formula = cbind(rank, outcome) ~ income + group,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  ntree = 10L,
  mtry = 1L,
  control = ci_tree_control(minsplit = 1, minbucket = 1, maxdepth = 1)
)

nrow(stats::fitted(fit))
}
