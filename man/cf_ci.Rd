% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_wt.R
\name{cf_ci}
\alias{cf_ci}
\title{Fit a conditional inference forest using concentration-index split scoring}
\usage{
cf_ci(
  formula,
  data,
  rank_name,
  outcome_name,
  weights = NULL,
  type = c("CI", "CIg", "CIc"),
  control = partykit::ctree_control(),
  ntree = 500L,
  mtry = NULL,
  perturb = list(replace = FALSE, fraction = 0.632),
  na.action = stats::na.omit,
  ...
)
}
\arguments{
\item{formula}{A model formula, typically with a two-column response such as
\code{cbind(rank, outcome) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{rank_name}{Name of the socioeconomic rank variable.}

\item{outcome_name}{Name of the outcome variable.}

\item{weights}{Optional case weights passed to \code{\link[partykit:cforest]{partykit::cforest()}}.}

\item{type}{One of \code{"CI"}, \code{"CIg"}, or \code{"CIc"} selecting the concentration
index variant used for split scoring.}

\item{control}{A control object, typically created by
\code{\link[partykit:ctree_control]{partykit::ctree_control()}}.}

\item{ntree}{Number of trees to grow.}

\item{mtry}{Optional number of variables randomly tried at each split.}

\item{perturb}{Resampling specification passed to \code{\link[partykit:cforest]{partykit::cforest()}}.}

\item{na.action}{A function for handling missing values.}

\item{...}{Additional arguments passed to \code{\link[partykit:cforest]{partykit::cforest()}}.}
}
\value{
A fitted forest object returned by \code{\link[partykit:cforest]{partykit::cforest()}}.

nrow(stats::fitted(fit))
}
\description{
Wrapper around \code{\link[partykit:cforest]{partykit::cforest()}} that uses \code{\link[=ci_splitfun]{ci_splitfun()}} to score
candidate splits by reduction in within-node concentration index.
}
\details{
This helper extends \code{\link[=ctree_ci]{ctree_ci()}} to an ensemble setting, allowing
inequality-aware tree splits to be aggregated across many trees.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12),
  group = factor(c("low", "low", "mid", "mid", "high", "high"))
)

fit <- cf_ci(
  formula = cbind(rank, outcome) ~ income + group,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  ntree = 10L,
  mtry = 1L,
  control = partykit::ctree_control(mincriterion = 0, maxdepth = 1)
)

nrow(stats::fitted(fit))
}
