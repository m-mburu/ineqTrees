% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ci_forest.R
\name{ci_forest}
\alias{ci_forest}
\alias{cf_ci}
\title{Fit a greedy concentration-index forest}
\usage{
ci_forest(
  formula,
  data,
  rank_name,
  outcome_name,
  weights = NULL,
  type = c("CI", "CIg", "CIc", "L"),
  control = ci_tree_control(),
  ntree = 500L,
  mtry = NULL,
  perturb = list(replace = FALSE, fraction = 0.632),
  parallel = FALSE,
  future.seed = TRUE,
  na.action = stats::na.omit,
  ...
)

cf_ci(...)
}
\arguments{
\item{formula}{A model formula, typically with a two-column response such as
\code{cbind(rank, outcome) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{rank_name}{Name of the socioeconomic rank variable.}

\item{outcome_name}{Name of the outcome variable.}

\item{weights}{Optional non-negative case weights.}

\item{type}{One of \code{"CI"}, \code{"CIg"}, \code{"CIc"}, or \code{"L"} selecting the
inequality index used for split scoring. \code{"L"} uses observed
socioeconomic levels in the first response column rather than fractional
ranks.}

\item{control}{A control object created by \code{\link[=ci_tree_control]{ci_tree_control()}}. Objects from
\code{\link[partykit:ctree_control]{partykit::ctree_control()}} are accepted for their shared controls, but
conditional-inference test controls such as \code{mincriterion} are ignored by
the greedy tree builder.}

\item{ntree}{Number of trees to grow.}

\item{mtry}{Optional number of variables randomly tried at each split.}

\item{perturb}{Resampling specification. Use \code{NULL} for the default
subsample or a list with optional \code{replace} and \code{fraction} entries.
\code{replace} controls whether rows are sampled with replacement and must be
\code{NULL} or a single non-missing logical. \code{fraction} controls the sample
size relative to the complete analysis data and must be \code{NULL} or a
positive finite number.}

\item{parallel}{Logical; grow trees with \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}.
This requires the suggested package \code{future.apply}. The future backend is
controlled by the user outside \code{ci_forest()}, for example with
\code{future::plan()}.}

\item{future.seed}{Passed to \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} when
\code{parallel = TRUE}.}

\item{na.action}{A function for handling missing values.}

\item{...}{Currently ignored; retained for backwards compatibility.}
}
\value{
A fitted \code{ci_forest} object containing the greedy trees, in-bag
indicators, fitted values, and forest controls.

A fitted \code{ci_forest} object.
}
\description{
Grow an ensemble of greedy concentration-index trees. Each tree uses the same
greedy tree builder as \code{\link[=ci_tree]{ci_tree()}} on a perturbed sample of the training
data, so the split at each node is chosen by directly maximizing
concentration-index gain over candidate variables and split points. The
optional \code{mtry} control randomly samples candidate variables at each node
inside the greedy split search.

\code{cf_ci()} is kept for existing code. New code should use \code{\link[=ci_forest]{ci_forest()}},
because the current forest is an ensemble of greedy concentration-index
trees rather than a conditional-inference forest.
}
\details{
\code{ci_forest()} is an ensemble of greedy CI trees, not a
conditional-inference forest. Randomness enters through row perturbation and,
optionally, through \code{mtry}, which restricts the number of predictors searched
at each node. Within each tree, the split rule is the same direct
maximization of concentration-index gain used by \code{\link[=ci_tree]{ci_tree()}}. Predictions are
aggregated across trees by averaging terminal-node summaries. The fitted
object retains the member trees in \code{object$trees}; model-selection helpers
use those internal tree partitions to compute forest-native validation gain.
}
\examples{
if (requireNamespace("future", quietly = TRUE) &&
    requireNamespace("future.apply", quietly = TRUE)) {
  old_plan <- future::plan()
  on.exit(future::plan(old_plan), add = TRUE)
  future::plan(future::sequential)

  fit <- ci_forest(
    cbind(rank, outcome) ~ income,
    data = data.frame(
      rank = c(10, 20, 30, 40, 50, 60),
      outcome = c(1, 0, 1, 0, 1, 1),
      income = c(2, 4, 6, 8, 10, 12)
    ),
    rank_name = "rank",
    outcome_name = "outcome",
    ntree = 3,
    parallel = TRUE,
    control = ci_tree_control(minsplit = 1, minbucket = 1, maxdepth = 1)
  )

  nrow(stats::fitted(fit))
}

}
\references{
Breiman L (2001). "Random Forests." \emph{Machine Learning}, 45, 5-32.

Breiman L, Friedman JH, Olshen RA, Stone CJ (1984). \emph{Classification and
Regression Trees}. Wadsworth.

Hothorn T, Zeileis A (2015). "partykit: A Modular Toolkit for Recursive
Partytioning in R." \emph{Journal of Machine Learning Research}, 16, 3905-3909.
https://jmlr.org/papers/v16/hothorn15a.html.
}
