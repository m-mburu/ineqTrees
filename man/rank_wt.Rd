% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_wt.R
\name{rank_wt}
\alias{rank_wt}
\title{Compute weighted fractional ranks}
\usage{
rank_wt(x, wt)
}
\arguments{
\item{x}{A numeric vector to rank.}

\item{wt}{A numeric vector of case weights with the same length as \code{x}.}
}
\value{
A numeric vector of weighted fractional ranks in \verb{(0, 1)}, one for
each retained observation. The output is ordered like the retained inputs
after removing cases with missing \code{x}, missing \code{wt}, or \code{wt <= 0}.
}
\description{
Compute midpoint fractional ranks after sorting \code{x} in ascending order and
normalizing positive weights to sum to one.
}
\details{
Observations with missing \code{x}, missing \code{wt}, or non-positive \code{wt} are
removed before ranking. The resulting ranks are useful as a low-level
building block for concentration-index calculations and related inequality
measures.
}
\examples{
rank_wt(c(10, 20, 30), c(1, 1, 2))
rank_wt(c(1, 2, NA, 3), c(1, 0, 1, 2))
}
