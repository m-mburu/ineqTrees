% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assert_data_table_available.R
\name{.validate_shap_table}
\alias{.validate_shap_table}
\title{Validate a SHAP table
Ensure that the input SHAP table is a data.table with at least one row and
one numeric column. Optionally check for missing values and trigger an error
any are found.}
\usage{
.validate_shap_table(shap_dt, allow_missing = FALSE)
}
\arguments{
\item{shap_dt}{A data.table representing SHAP values to validate.}

\item{allow_missing}{Logical scalar. If \code{TRUE}, allow missing values in
SHAP columns. If \code{FALSE}, trigger an error if any SHAP column contains
missing values.}
}
\value{
The input \code{shap_dt} if it passes validation checks.
}
\description{
Validate a SHAP table
Ensure that the input SHAP table is a data.table with at least one row and
one numeric column. Optionally check for missing values and trigger an error
any are found.
}
\details{
This function is used internally to ensure that SHAP tables are in
a consistent format with appropriate types before decomposition. It checks
that the input is a data.table (or coercible to one), contains at least
one row and one column, that all columns are numeric, and optionally that
there are no missing values.
}
