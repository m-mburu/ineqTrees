% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_wt.R
\name{ctree_ci}
\alias{ctree_ci}
\title{Fit a conditional inference tree using concentration-index split scoring}
\usage{
ctree_ci(
  formula,
  data,
  rank_name,
  outcome_name,
  weights = NULL,
  type = c("CI", "CIg", "CIc"),
  control = partykit::ctree_control(),
  na.action = stats::na.omit,
  ...
)
}
\arguments{
\item{formula}{A model formula, typically with a two-column response such as
\code{cbind(rank, outcome) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{rank_name}{Name of the socioeconomic rank variable.}

\item{outcome_name}{Name of the outcome variable.}

\item{weights}{Optional non-negative integer case weights. Non-integer
weights must be converted before fitting because \code{\link[partykit:ctree]{partykit::ctree()}}
accepts integer case weights only.}

\item{type}{One of \code{"CI"}, \code{"CIg"}, or \code{"CIc"} selecting the concentration
index variant used for split scoring.}

\item{control}{A control object created by \code{\link[partykit:ctree_control]{partykit::ctree_control()}}.}

\item{na.action}{A function for handling missing values.}

\item{...}{Additional arguments passed to \code{\link[partykit:ctree]{partykit::ctree()}}.}
}
\value{
A fitted \code{party} object returned by \code{\link[partykit:ctree]{partykit::ctree()}}.
}
\description{
Wrapper around \code{\link[partykit:ctree]{partykit::ctree()}} that replaces the default split-selection
rule with a concentration-index-based split function created by
\code{\link[=ci_splitfun]{ci_splitfun()}}.
}
\details{
This helper is intended for inequality-aware tree fitting where the response
contains a socioeconomic ranking variable and a health outcome, and splits
are chosen to reduce within-node inequality rather than standard predictive
loss.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12),
  group = factor(c("low", "low", "mid", "mid", "high", "high"))
)

fit <- ctree_ci(
  formula = cbind(rank, outcome) ~ income + group,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control = partykit::ctree_control(mincriterion = 0, maxdepth = 1)
)

inherits(fit, "party")
}
