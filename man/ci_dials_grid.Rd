% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_ci_tree_terminal_mean.R
\name{ci_dials_grid}
\alias{ci_dials_grid}
\title{Convert a dials-style grid to greedy CI controls}
\usage{
ci_dials_grid(
  grid,
  minsplit = NULL,
  minbucket = NULL,
  minprob = 0.01,
  maxdepth = NULL,
  min_gain = 0,
  min_relative_gain = 0,
  mtry = NULL,
  ntree = NULL
)
}
\arguments{
\item{grid}{A data frame or tibble, typically from \code{dials::grid_regular()}.}

\item{minsplit}{Optional parent-node sizes. If \code{NULL}, a \code{minsplit} column is
used when present; otherwise \code{2 * minbucket} is used.}

\item{minbucket}{Optional child-node sizes. If \code{NULL}, a \code{minbucket} column
or dials \code{min_n} column is used.}

\item{minprob}{Minimum child-node weight proportions.}

\item{maxdepth}{Optional maximum tree depths. If \code{NULL}, a \code{maxdepth} column
or dials \code{tree_depth} column is used, falling back to the
\code{\link[=ci_tree_control]{ci_tree_control()}} default.}

\item{min_gain}{Minimum concentration-index gains required for a split.}

\item{min_relative_gain}{Minimum concentration-index gains as a share of
parent-node impurity required for a split.}

\item{mtry}{Optional numbers of variables sampled at each split. If \code{NULL},
an \code{mtry} column is used when present.}

\item{ntree}{Optional numbers of trees for forest tuning. If \code{NULL}, an
\code{ntree} or dials \code{trees} column is used when present.}
}
\value{
A \code{data.table} with one row per candidate control setting.
}
\description{
Converts a data frame produced by tools such as \code{dials::grid_regular()} into
the control-grid columns expected by \code{\link[=tune_ci_tree]{tune_ci_tree()}} and \code{\link[=tune_ci_forest]{tune_ci_forest()}}.
The helper keeps \code{ineqTrees} independent of tidymodels while making common
dials names usable in package tuning workflows.

Recognized input columns are \code{min_n} or \code{minbucket}, \code{tree_depth} or
\code{maxdepth}, \code{mtry}, \code{trees} or \code{ntree}, \code{minsplit}, \code{minprob}, and
\code{min_gain}, and \code{min_relative_gain}. When \code{minsplit} is not supplied, it
defaults to twice the child node size (\code{minbucket}), which is a common
starting point for binary splits.
}
\examples{
if (requireNamespace("dials", quietly = TRUE)) {
  dials_grid <- dials::grid_regular(
    dials::mtry(range = c(2L, 4L)),
    dials::min_n(range = c(20L, 60L)),
    dials::tree_depth(range = c(2L, 3L)),
    levels = 2L
  )

  ci_dials_grid(dials_grid, minprob = 0.05, ntree = 10L)
}

}
