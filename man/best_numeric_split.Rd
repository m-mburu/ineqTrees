% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/best_numeric_split.R
\name{best_numeric_split}
\alias{best_numeric_split}
\title{Find the best numeric split by concentration-index gain}
\usage{
best_numeric_split(
  x,
  y,
  wt,
  varid,
  ctrl,
  ci_fun,
  return = c("split", "candidate")
)
}
\arguments{
\item{x}{A numeric predictor vector.}

\item{y}{A two-column numeric matrix whose first column is the socioeconomic
ranking variable and whose second column is the health outcome.}

\item{wt}{A numeric vector of case weights with the same length as \code{x}.}

\item{varid}{Integer identifier of the splitting variable, passed to
\code{\link[partykit:partysplit]{partykit::partysplit()}}.}

\item{ctrl}{A list-like control object containing \code{minbucket} and \code{minprob}.}

\item{ci_fun}{A concentration-index scoring function, typically created by
\code{\link[=ci_factory]{ci_factory()}}.}

\item{return}{Either \code{"split"} to return only a \code{\link[partykit:partysplit]{partykit::partysplit()}}
object, or \code{"candidate"} to also return the gain and left-child routing
needed by the greedy tree builder.}
}
\value{
A \code{partysplit} object describing the best admissible binary split, or
\code{NULL} if no split is available because all \code{x} values are tied or all
candidate splits violate the control constraints.
}
\description{
Evaluate all admissible cutpoints of a numeric predictor and return the
split with the largest weighted concentration-index gain.
}
\details{
Candidate cutpoints are formed at midpoints between distinct sorted values of
\code{x}. Each candidate is filtered using \code{ctrl$minbucket} and \code{ctrl$minprob}
before being scored with \code{\link[=weighted_ci_gain]{weighted_ci_gain()}}.
}
\examples{
x <- c(2, 4, 6, 8, 10)
y <- cbind(
  ses = c(10, 20, 30, 40, 50),
  health = c(1, 0, 1, 0, 1)
)
wt <- rep(1, 5)
ctrl <- list(minbucket = 1, minprob = 0)

split <- best_numeric_split(
  x = x,
  y = y,
  wt = wt,
  varid = 1,
  ctrl = ctrl,
  ci_fun = ci_factory("CI")
)
split$breaks
}
