% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tree_default_name.R
\name{plot.ci_tree}
\alias{plot.ci_tree}
\title{Plot a conditional inference tree with labeled split and node panels}
\usage{
\method{plot}{ci_tree}(
  x,
  main = NULL,
  terminal_panel = NULL,
  tp_args = list(),
  inner_panel = NULL,
  ip_args = list(),
  edge_panel = NULL,
  ep_args = list(),
  data = NULL,
  terminal_stats = list(n = function(data) NROW(data)),
  var_labels = NULL,
  stat_labels = NULL,
  stat_formatters = list(),
  show_p = TRUE,
  edge_fill = "white",
  inner_fill = "white",
  terminal_fill = "lightgray",
  ...
)
}
\arguments{
\item{x}{A fitted \code{ci_tree} object created by \code{\link[=ctree_ci]{ctree_ci()}}.}

\item{main}{Optional main title passed to the underlying plot method.}

\item{terminal_panel, inner_panel, edge_panel}{Optional panel functions passed
through to \code{partykit} plotting.}

\item{tp_args, ip_args, ep_args}{Lists of extra arguments for the panel
generators.}

\item{data}{Optional data used to compute terminal-node summaries. Defaults
to the model frame stored on \code{x}.}

\item{terminal_stats}{Named list of summary functions for terminal nodes. Use
\code{NULL} to fall back to the default \code{partykit} terminal panel.}

\item{var_labels}{Optional named character vector mapping variable names to
prettier labels in split and inner-node annotations.}

\item{stat_labels}{Optional named character vector for terminal summary
labels.}

\item{stat_formatters}{Optional named list of formatting functions for
terminal summaries.}

\item{show_p}{Logical; include split-test p-values in internal nodes.}

\item{edge_fill, inner_fill, terminal_fill}{Fill colors for the custom panels.}

\item{...}{Additional arguments passed to \code{\link[partykit:party-plot]{partykit::plot.party()}}.}
}
\value{
Invisibly returns the plotted \code{ci_tree}.
}
\description{
\code{ctree_ci()} returns objects with class \code{ci_tree}, so calling \code{plot()} on a
fitted tree dispatches to this method. The method keeps the underlying
\code{partykit} layout while supplying compact edge labels, labeled internal
nodes, and optional terminal-node summaries by default.
}
\examples{
toy_data <- data.frame(
  rank = c(10, 20, 30, 40, 50, 60),
  outcome = c(1, 0, 1, 0, 1, 1),
  income = c(2, 4, 6, 8, 10, 12),
  group = factor(c("low", "low", "mid", "mid", "high", "high"))
)

fit <- ctree_ci(
  formula = cbind(rank, outcome) ~ income + group,
  data = toy_data,
  rank_name = "rank",
  outcome_name = "outcome",
  control = ci_tree_control(minsplit = 1, minbucket = 1, maxdepth = 1)
)

plot(fit)
plot(
  fit,
  data = toy_data,
  terminal_stats = list(
    n = nrow,
    mean_outcome = function(df) mean(df$outcome)
  )
)
}
