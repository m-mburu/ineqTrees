% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_ci_forest.R, R/tune_ci_tree.R
\name{tune_ci_forest}
\alias{tune_ci_forest}
\alias{tune_cf_ci}
\title{Tune greedy concentration-index forest controls by cross-validation}
\usage{
tune_cf_ci(...)

tune_ci_forest(
  formula,
  data,
  rank_name,
  outcome_name,
  weights = NULL,
  type = c("CI", "CIg", "CIc", "L"),
  control_grid = NULL,
  ntree = 500L,
  v = 5L,
  strata = NULL,
  fold_id = NULL,
  resamples = NULL,
  seed = NULL,
  metric = c("validation_gain", "brier", "log_loss", "roc_auc"),
  metrics = NULL,
  surrogate_control = NULL,
  prediction_name = "forest_risk",
  refit = TRUE,
  verbose = FALSE,
  control = NULL,
  perturb = list(replace = FALSE, fraction = 0.632),
  parallel_over = c("none", "tuning", "forest"),
  future.seed = TRUE,
  na.action = stats::na.omit,
  ...
)
}
\arguments{
\item{...}{Additional arguments passed to \code{\link[=ci_tree]{ci_tree()}}.}

\item{formula}{A model formula, typically with a two-column response such as
\code{cbind(rank, outcome) ~ x1 + x2}.}

\item{data}{A data frame containing the variables in \code{formula}.}

\item{rank_name}{Name of the socioeconomic rank variable.}

\item{outcome_name}{Name of the outcome variable.}

\item{weights}{Optional non-negative case weights.}

\item{type}{One or more of \code{"CI"}, \code{"CIg"}, \code{"CIc"}, or \code{"L"} selecting
candidate inequality objectives. \code{"L"} uses observed socioeconomic levels
in the first response column rather than fractional ranks.}

\item{control_grid}{A data frame of candidate controls. Defaults to
\code{\link[=ci_tree_control_grid]{ci_tree_control_grid()}}. If it contains a \code{type} column, that column is
used instead of expanding over \code{type}.}

\item{ntree}{Number of trees used when \code{control_grid} does not contain an
\code{ntree} column.}

\item{v}{Number of cross-validation folds.}

\item{strata}{Optional stratum vector, or a single column name in \code{data},
used for stratified fold creation.}

\item{fold_id}{Optional precomputed integer fold ids. When supplied, \code{v},
\code{strata}, and \code{seed} are ignored for fold creation.}

\item{resamples}{Optional \code{rsample} rset object. When supplied, it is used
instead of creating folds from \code{v}, \code{strata}, or \code{fold_id}.}

\item{seed}{Optional random seed for fold creation.}

\item{metric}{Selection metric when a single metric is requested.
\code{validation_gain} and \code{roc_auc} are maximized; \code{brier} and \code{log_loss} are
minimized.}

\item{metrics}{Optional character vector of one or more metrics to compute
during tuning. When supplied, the first metric is used for final model
selection.}

\item{surrogate_control}{Optional \code{\link[=ci_tree_control]{ci_tree_control()}} for the surrogate tree.
By default, the selected forest controls are reused with \code{mtry = NULL} so
the surrogate searches all split variables.}

\item{prediction_name}{Name of the forest-prediction column added to the
surrogate data.}

\item{refit}{Should the best setting be refitted on the full data?}

\item{verbose}{Should fold progress be printed?}

\item{control}{Optional \code{\link[=control_ci_tune]{control_ci_tune()}} object controlling saved
predictions, saved fits, extraction hooks, and parallel settings.}

\item{perturb}{Resampling controls passed to \code{\link[=ci_forest]{ci_forest()}}.}

\item{parallel_over}{Parallelization strategy. \code{"none"} runs tuning and
forest fitting serially, \code{"tuning"} evaluates grid/resample tasks with
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}, and \code{"forest"} keeps tuning tasks serial
while growing trees inside each \code{\link[=ci_forest]{ci_forest()}} call with
\code{\link[future.apply:future_lapply]{future.apply::future_lapply()}}. The future backend is controlled by the
user outside \code{tune_ci_forest()}.}

\item{future.seed}{Passed to \code{\link[future.apply:future_lapply]{future.apply::future_lapply()}} when
\code{parallel_over} is \code{"tuning"} or \code{"forest"}.}

\item{na.action}{A function for handling missing values.}
}
\value{
A \code{ci_forest_tuning} object.

A \code{ci_forest_tuning} object.
}
\description{
\code{tune_cf_ci()} is kept for existing code. New code should use
\code{\link[=tune_ci_forest]{tune_ci_forest()}}.

Fits \code{\link[=ci_forest]{ci_forest()}} across candidate forest controls and concentration-index
criteria. Each validation forest is summarized by a surrogate greedy
\code{\link[=ci_tree]{ci_tree()}} fitted to the forest predictions on the training fold, then
scored by held-out concentration-index validation gain against the observed
outcome. Prediction-oriented metrics can be computed from the forest
predictions in the same tuning run.
}
\examples{
if (requireNamespace("future", quietly = TRUE) &&
    requireNamespace("future.apply", quietly = TRUE)) {
  old_plan <- future::plan()
  future::plan(future::multisession, workers = 2)
  on.exit(future::plan(old_plan), add = TRUE)

  toy_data <- data.frame(
    rank = c(10, 20, 30, 40, 50, 60, 70, 80),
    outcome = c(1, 0, 1, 0, 1, 1, 0, 1),
    income = c(2, 4, 6, 8, 10, 12, 14, 16)
  )
  grid <- ci_tree_control_grid(
    minsplit = 1,
    minbucket = 1,
    minprob = 0,
    maxdepth = 1,
    ntree = 3
  )
  tuned <- tune_ci_forest(
    cbind(rank, outcome) ~ income,
    data = toy_data,
    rank_name = "rank",
    outcome_name = "outcome",
    control_grid = grid,
    v = 2,
    parallel_over = "tuning",
    refit = FALSE
  )
}

}
