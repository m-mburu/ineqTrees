% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_ci_tree_terminal_mean.R
\name{ci_tree_control_grid}
\alias{ci_tree_control_grid}
\title{Create a tuning grid for greedy concentration-index trees and forests}
\usage{
ci_tree_control_grid(
  minsplit = c(50L, 100L, 200L),
  minbucket = c(20L, 50L, 100L),
  minprob = c(0.01),
  maxdepth = c(2L, 3L, 4L),
  min_gain = 0,
  min_relative_gain = 0,
  mtry = NULL,
  ntree = NULL
)
}
\arguments{
\item{minsplit}{Candidate minimum weighted parent-node sizes.}

\item{minbucket}{Candidate minimum weighted child-node sizes.}

\item{minprob}{Candidate minimum child-node weight proportions.}

\item{maxdepth}{Candidate maximum tree depths.}

\item{min_gain}{Candidate minimum concentration-index gains required for a
split.}

\item{min_relative_gain}{Candidate minimum concentration-index gains as a
share of parent-node impurity required for a split.}

\item{mtry}{Optional candidate numbers of variables sampled at each node.
Use \code{NULL} to search all candidate variables.}

\item{ntree}{Optional candidate numbers of trees for forest tuning.}
}
\value{
A \code{data.table} with one row per candidate control setting.
}
\description{
Builds a grid of candidate controls for \code{\link[=ci_tree]{ci_tree()}} and \code{\link[=ci_forest]{ci_forest()}}. The grid
is designed for the fully greedy tree builder, so it tunes tree-size and
split-search controls rather than conditional-inference test controls such
as \code{alpha} or \code{mincriterion}.
}
\examples{
ci_tree_control_grid(
  minsplit = c(20, 50),
  minbucket = c(5, 10),
  maxdepth = 2:3
)

}
