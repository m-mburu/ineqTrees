% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rank_wt.R
\name{ci_factory}
\alias{ci_factory}
\title{Create concentration-index scoring functions}
\usage{
ci_factory(type = c("CI", "CIg", "CIc"))
}
\arguments{
\item{type}{One of \code{"CI"}, \code{"CIg"}, or \code{"CIc"} selecting the standard,
generalized, or corrected concentration index.}
}
\value{
A function with signature \verb{function(y, wt)} where \code{y} is a two-column
numeric matrix and \code{wt} is a vector of positive case weights. The returned
function drops incomplete rows and non-positive weights, then returns a
single non-negative concentration-index value. It returns \code{0} when fewer
than two valid observations remain or when the required denominator is
degenerate.
}
\description{
Build a function that computes a weighted concentration-index criterion from
a two-column response matrix. The first column is treated as the
socioeconomic ranking variable and the second as the health outcome.
}
\details{
This factory is intended for inequality-aware split scoring, where different
concentration-index variants can be swapped in while keeping a common
\verb{function(y, wt)} interface.

The returned function first converts \code{y[, 1]} to weighted
fractional ranks using \code{\link[=rank_wt]{rank_wt()}}. It then computes the weighted
covariance between rank and outcome and maps it to the requested index:
\code{"CI"} divides by the weighted mean of the outcome, \code{"CIg"} returns the
generalized form, and \code{"CIc"} applies an Erreygers-style range correction.
Absolute values are returned.
}
\examples{
ci <- ci_factory("CI")
y <- cbind(c(10, 20, 30), c(1, 0, 1))
ci(y, c(1, 1, 2))
cig <- ci_factory("CIg")
cig(y, c(1, 1, 2))
cic <- ci_factory("CIc")
cic(y, c(1, 1, 2))
}
